{#
/**
 * @file
 * Kiso's theme override for a date / time element.
 *
 * You will find same variables as in the core 'time.html.twig' template.
 *
 * Related WCAG resources:
 * - SC 1.3.1 Info and Relationships (Level A):
 *   - Providing date and time information using the time element.
 *
 * @see template_preprocess_time()
 * @see http://www.w3.org/TR/html5-author/the-time-element.html#attr-time-datetime
 */
#}
<time{{ attributes.addClass('datetime') }}>{{ text }}</time>
