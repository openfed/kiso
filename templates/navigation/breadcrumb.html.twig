{#
/**
 * @file
 * Kiso's theme override to display a breadcrumb trail.
 *
 * You will find same variables as in the core 'breadcrumb.html.twig' template.
 *
 * Related WCAG resources:
 * - SC 1.3.1 Info and Relationships (Level A):
 *   - H48: Using ol, ul and dl for lists or groups of links.
 *   - ARIA11: Using ARIA landmarks to identify regions of a page.
 * - SC 2.4.8 Location (Level AAA):
 *   - G65: Providing a breadcrumb trail.
 *   - G128: Indicating current location within navigation bars.
 * - SC 4.1.2 Name, Role, Value (Level A):
 *   - ARIA14: Using aria-label to provide an invisible label where a visible
 *     label cannot be used.
 */
#}
{% set container = container_fluid ? 'container-fluid' : 'container' %}

{% if not is_front and breadcrumb %}
  <nav class="breadcrumb {{ container }}" aria-label="{{ 'Breadcrumb'|t }}">
    <ol>
      <li><a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">{{ 'Home'|t }}</a></li>
      {% for item in breadcrumb %}
        <li>
          {%- if item.url -%}
            <a href="{{ item.url }}" title="{{ item.text }}">{{ item.text }}</a>
          {%- else -%}
            <span class="visually-hidden">{{ 'Current Page'|t }}:</span>
            {{- item.text -}}
          {%- endif -%}
        </li>
      {% endfor %}
    </ol>
  </nav>
{% endif %}
